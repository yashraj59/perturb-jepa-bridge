# Identity Violations Considered

Rejected:

- continuing BMJ001 longer by default;
- launching BMJ002-BMJ006;
- using raw-linear PLS as the main representation path;
- using `condition_key`, `biological_key`, exact target-key one-hot, or test target means;
- pooling train and test target latents for delta whitening or baseline fitting;
- using batch id as a biological transition shortcut;
- claiming Norman validates imaging or batch disentanglement.
